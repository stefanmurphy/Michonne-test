name: Merge staging branch to biz
on: [push, pull_request, issues]
#on:
#  push:
#    branches:
#      - 'master'

jobs:
  merge-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Merge to uat branch
        uses: devmasx/merge-branch@v1.1.0
        with:
          type: now
          target_branch: 'biz'
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
